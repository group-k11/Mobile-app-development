import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ShelfSense app placeholder test', (WidgetTester tester) async {
    // Placeholder test — full widget tests require Firebase mocks.
    expect(1 + 1, equals(2));
  });
}
